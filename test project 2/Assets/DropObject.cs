using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DropObject : MonoBehaviour // every script dervies from this class
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       /* if (Input.GetKeyDown(KeyCode.Space)) // Input is another magic variable that gets the players current input
        {
            Rigidbody rb = gameObject.GetComponent<Rigidbody>(); // gameObject will be whatever game object the script is attached to in the Unity editor
            rb.useGravity = true; // matches "Use Gravity" checkbox in the editor
        }*/
    }
}
